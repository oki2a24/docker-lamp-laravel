version: '3.5'

networks:
  frontend:
    name: fronted
    driver: bridge
  backend:
    name: backend
    driver: bridge

volumes:
  db_data:
    driver: local

services:
  php_apache:
    build:
      context: ./php_apache
    ports:
      - 80:80
      - 443:443
        #    env_file: .env
        #    environment:
        #      DATABASE_HOST: db
        #    depends_on:
        #      - db
    volumes:
      - ./data/html:/var/www/html

        #  db:
        #    build: ./db
        #    env_file: .env
        #    ports:
        #      - 3306:3306
        #    volumes:
        #      - db_data:/var/lib/mysql
        #      - ./db/initial.sql:/docker-entrypoint-initdb.d/initial.sql
